# 通用请求恢复验证记录

本记录仅描述实际执行结果，不代表真实 CLI 端到端验收通过。未修改版本、发布、打包或安装。

## 已执行

- VS Code MCP 诊断：0 errors、0 warnings；有未使用符号和弃用 API 的 hint。
- TypeScript 编译通过。
- `node --check media/chat/main.js` 通过。
- `npm test`：2026-09-11 本轮重新执行，377 项通过，0 失败、0 取消、0 跳过；耗时约 5.36 秒。原始输出：`/tmp/lls-recovery-task9-test.log`（临时文件，非持久验收附件）。
- 扩展 `src/relay/__tests__/recoveryIntegration.test.ts`：真实本地 socket、假上游和 HTTP 客户端模拟 CLI 网络侧，经过三种生产适配器，覆盖 HTTP 429 + Retry-After、HTTP 502 和 HTTP 200 + SSE 内嵌错误，共九种组合。验证单次上报、原始状态、错误码和等待时间；服务均已关闭。
- 新增控制器回归：压缩与权限同时阻塞，必须全部解除才能安排恢复；取消后迟到解除阻塞及失败结果不得复活队列。
- 执行顺序：先 VS Code MCP 诊断，再 TypeScript 编译、前端语法检查、全量 npm test；修改测试后按同一顺序重新执行，全部通过。

## 已有自动回归覆盖

- 虚拟时钟：五档退避、第五次失败后耗尽、手动重试旧身份拒绝。
- 原生 retry 不立即恢复、请求在途和权限阻塞暂停、异步恢复取消信号、跨会话/代数迟到事件拒绝。
- 首请求停滞恢复与新活动撤销；未知错误不自动重试。
- CLI 合成夹具：局部 message_stop 非终止、空正文失败 result、未知等待时间、最后 attempt 不代表结束。
- 任务流：恢复互斥、重复意图合并、beforeSubmit 返回后二次守卫。
- UI：七语言格式化、倒计时归零仅显示、不触发发送；成功/取消隐藏状态。
- Relay：分片 SSE（含逐字符 CRLF）、error:null 不误判、真实 Responses 判断方法回归、取消和超时证据区别。

## 未验证及已知限制

- 没有启动真实 CLI 子进程做重试耗尽、原会话恢复、权限弹窗与压缩的端到端验证；HTTP 假客户端不能代替真实 CLI 验收。
- 未完成假 CLI 子进程驱动完整 activation → send → result → recovery 的闭环测试。
- 未对三种协议逐一执行连接拒绝、首字节真实超时、流空闲真实超时与取消竞态的全矩阵 socket 测试；现有单元测试不能代替这些集成场景。
- 未做浏览器 DOM 交互验收；目前 UI 测试只覆盖实际格式化函数，不证明按钮与布局端到端正确。
- 已补充首次 session/init 的单次身份绑定及旧身份拒绝测试；实际 CLI 事件时序仍需端到端验证。
- 已将每次进入 awaiting_request 的观察计时统一接入状态通知，恢复发送后重新计时；完整运行期闭环仍待验证。
- 同一会话不同回合的 Relay 请求目前以 sessionId 关联入口，仍需加强 CLI 实例/回合关联，不能宣称完全杜绝迟到请求污染。
- 本地停滞标记后，迟到工具/压缩事件、CLI 最终结果与恢复调度的全部组合未完整验证。

因此当前结论是编译和现有回归通过，但完整方案验收仍有上述缺口，不建议把测试通过理解为可以直接发布。
