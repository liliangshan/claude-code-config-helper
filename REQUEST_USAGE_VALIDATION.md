# 请求级用量展示验证记录

## 本地验证结果

- VS Code MCP：0 错误、0 警告；存在未使用符号和弃用 API 的提示级诊断。
- `node --check media/chat/main.js` 通过。
- `npm test`（含 TypeScript 编译）：359 个测试全部通过。

## 新增/执行的回归覆盖

- 请求身份唯一且冻结；缺失会话不猜测绑定。
- 输入组成、缺失值、零值、非法数值及命中率计算。
- 请求 usage 分片聚合、无 usage、超时、中断、重复结束与 sink 失败。
- 并发报告倒序、跨会话隔离、身份冲突、清理后的迟到报告和完成记录裁剪。
- CLI 流式与 SDK 响应 message.id 透传。
- 七种语言的五字段格式化、不展示总输入、缺失值与中断文案。
- 全量执行既有 Chat/Responses JSON 与 SSE 缓存归一化测试。

## 本地浏览器验证

使用实际 main.js/style.css/index.html 的临时副本，替换 VS Code bridge 为 mock。原始模板直接加载因 CSP 占位符不可用，故仅在临时测试副本中移除 CSP；正式源码安全策略未改动。

- 三个片段分属两个 requestId，最终只有两个标题，每个标题恰好五项。
- request/usage 到达后定点显示 3,000 / 22,000 / 1,000 / 842 / 84.62%，无总输入。
- 切换到简体中文后保留已收到用量，另一请求仍显示未知值。
- 320px 宽度下字段分两行，统计容器无横向溢出。
- 临时 HTTP 服务验证后已停止。

## 未验证及限制

- 未连接真实上游执行三种协议的端到端 CLI 测试；实际 Relay message.id 是否在每种 CLI 路径原样保留，仍需安装后确认。
- 未验证真实并行子智能体、压缩和会话切换共同运行；无法可靠关联的输出按方案不猜配。
- 未完整自动化验证工具卡片展开状态、长历史滚动锚点和所有切换/重发组合；已有相关实现及单测不等于这些场景已端到端验收。
- 原始 CLI JSONL 没有扩展请求统计字段，直接导入该历史不能凭空恢复统计；扩展自身保存的 segment.requestUsage 可随历史恢复。
- 当前严格口径下，提供商未报告缓存写入时，命中率可能显示未知；不将缺失字段自动解释为零。
- 本次未改版本、打包或安装。后续发布前应针对上述限制进行真实协议验收。
